package model;
// Class for letters (yellow and green) with a specific position
public class PositionedLetter {
    public char letter;
    public int position;

    // Constructor for initializing a letter and its position
    public PositionedLetter(char letter, int position) {
        this.letter = letter;
        this.position = position;
    }
}
